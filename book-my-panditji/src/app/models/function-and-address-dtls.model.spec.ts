import { FunctionAndAddressDtls } from './function-and-address-dtls.model';

describe('FunctionAndAddressDtls', () => {
  it('should create an instance', () => {
    expect(new FunctionAndAddressDtls()).toBeTruthy();
  });
});
